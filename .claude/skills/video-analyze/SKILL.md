---
name: video-analyze
description: 動画（YouTube Shorts / Reels / TikTok / 自社の納品動画など）を「なぜ分かりやすいのか」「なぜ伸びたのか」「どこで離脱するか」の観点で構造分析するスキル。ユーザーが動画URLやファイルを渡して「この動画どう？」「分析して」「なんで分かりやすい？」「参考にしたい」と言ったら必ず使う。ネットワークで動画サイトに直接アクセスできない環境を前提に、Google Drive / Adobe 経由でファイルを受け取り、tools/video_analyze/analyze.py でショット・音声・文字起こしに分解してから、決まった分析フレームで読み解く。
---

# video-analyze

## 0. まず前提を言う

この環境（Claude Code on the web）は YouTube・TikTok・Instagram・Google 検索など外部サイトへの通信が組織ポリシーで遮断されている。URL だけ渡されても中身は取れない。**「見れない」で止めず、ファイルの受け取り経路を提示して、受け取り次第このスキルで分析する。**

受け取り経路（優先順）:

0. **Gemini API（URL のまま見られる唯一の経路）** … `generativelanguage.googleapis.com` はこの環境から到達できる。`GEMINI_API_KEY` が環境にあれば、YouTube URL をそのまま Gemini に渡して観察記録を取る:

   ```bash
   python3 tools/video_analyze/gemini_video.py "https://youtube.com/shorts/XXXX" --out <dir>
   # -> <dir>/observation.md（逐語の文字起こし・テロップ・ショット一覧・視覚イベント、全部タイムコード付き）
   python3 tools/video_analyze/gemini_video.py --list-models   # 使えるモデル名の確認
   ```

   役割分担を守る: **Gemini は目（観察）、分析は Claude が §3 のフレームでやる。** `--mode both` で Gemini 自身の分析も別ファイルに出せるが、それは参考意見として扱い、observation.md にない事実（チャンネルの出自、再生数、制作ツールの推測）は分析に混ぜない。ユーザーが Gemini の分析を貼ってきたときも同じで、観察記録と照合して裏が取れた部分だけ採用する。
   キーが無ければ、ユーザーに `GEMINI_API_KEY` を環境変数（Claude Code on the web の環境設定）に入れてもらうか、このセッション限りで export してもらう。

1. **Google Drive** … ユーザーが動画ファイル（mp4/mov）を Drive に置く → `mcp__Google_Drive__search_files` で `mimeType contains 'video/'` を検索 → `download_file_content` で base64 を受け取り、scratchpad に書き出す。Drive の Web UI は遮断されているが、MCP コネクタ経由の取得は通る。
2. **Adobe Creative Cloud** … `asset_add_file` でユーザーにファイルを選んでもらう → `video_render_frame` で任意タイムコードのフレーム、`media_summarize` で発話要約。ローカル分解ができないので Drive 経路の補助扱い。
3. **文字起こし＋スクショの貼り付け** … 動画本体が無理なら、ユーザーに YouTube の「文字起こしを表示」のコピーと、キーフレームのスクショを数枚もらう。分析フレーム（§3）はそのまま使える。

YouTube Shorts をファイルにする方法はユーザー側で判断してもらう（自分の動画なら YouTube Studio からダウンロード、他人の動画なら画面収録など）。権利上の注意は一言添える。

## 1. 分解する（ツール）

Gemini 経路で observation.md が取れた場合は §1 のローカル分解は不要（ファイルが無いので実行できない）。ファイルがある場合は両方やると、カット数や発話率の数値が取れて分析が固くなる。

```bash
# 初回のみ
pip install -r tools/video_analyze/requirements.txt
bash tools/video_analyze/setup_models.sh        # VAD + SenseVoice(日本語対応ASR) 約230MB

# 実行
python3 tools/video_analyze/analyze.py <video.mp4> --out <dir> --lang ja
```

出力（`<dir>/`）:

| ファイル | 中身 | 使い方 |
|---|---|---|
| `report.md` | 尺・解像度・ショット数・平均ショット長・10秒あたりカット数・発話率、ショット×文字起こしのタイムライン | まず読む |
| `report.json` | 上記の生データ（loudness_db は 0.25秒刻みの dBFS） | 数値が必要なとき |
| `contact_sheet_NN.jpg` | 1秒刻みのフレームをタイムコード・ショット番号・その時点の発話付きで並べた一覧 | **Read で画像として必ず読む**。テロップ・構図・表情はここから読む |
| `keyframes/shot_NNN_*.jpg` | 各ショットの代表フレーム（高さ640px） | テロップの文字や細部を確認するとき |

読む順番: `report.md` → コンタクトシート全部 → 気になるショットの keyframe。**画像を読まずに分析を書かない。**

オプション: `--interval 0.5`（テンポが速い動画）、`--cut-threshold 0.2`（カットを拾い損ねるとき）、`--no-asr`（文字起こし不要のとき）。

## 2. 分析の前に決めること

「分かりやすい」「伸びた」は結果であって原因ではない。原因を特定するために、分析の主語を先に決める:

- **誰向けの動画か**（視聴者の前提知識）
- **1本で伝えている主張は何か**（1文で書けなければ、分かりやすさの正体は「主張の単一性」ではない）
- **ユーザーが何に転用したいか**（cototo-film の採用動画？ Atacama のショート？ 自社LP？）

## 3. 分析フレーム（7観点・すべてタイムコード付きの根拠を書く）

各観点に「根拠（何秒に何があるか）」を必ず添える。根拠が書けない観点は「該当なし」と書く。印象語（テンポがいい、引き込まれる）だけの記述は禁止。

1. **冒頭2秒の設計（フック）**
   最初のフレームに主張・問い・違和感のどれがあるか。テロップと音声の初語は何か。「何の動画か」が0.5秒で判別できるか。
2. **主張の構造**
   結論→理由→具体例→再結論 / 問題→原因→解決 / 比較 / 手順、のどれか。転換点のタイムコード。1ショット1情報になっているか。
3. **三重符号化（音声・テロップ・映像の一致）**
   同じ情報を音声・テロップ・映像で同時に言っているか。テロップは発話の全文か、要約か（文字数）。矛盾している箇所はないか。
4. **カットとリズム**
   `cuts_per_10s`、平均ショット長、無音の有無（`speech_ratio`、VAD区間）。カットが「情報の切り替わり」と一致しているか、それとも装飾か。
5. **視覚の錨（アンカー）**
   数字・矢印・指差し・ビフォーアフター・図解など、目が止まる要素。画面のどこにあるか（縦動画の安全域）。
6. **認知負荷**
   1文の長さ、専門用語の数、代名詞の曖昧さ。難しい語をどう言い換えているか。
7. **離脱リスクと CTA**
   ダレる区間（ショット長が急に伸びる／発話が途切れる）、終わり方（CTA の有無と言い方）。

## 4. 出力の形

1. **結論を1段落**: 分かりやすさ（or 伸び）の主因を、7観点のうち上位2〜3つに絞って言い切る。
2. **根拠テーブル**: 観点 | 根拠（タイムコード＋何が起きているか） | 評価
3. **転用**: ユーザーの目的（§2）に対して、真似する技術3つと、真似してはいけない点（文脈依存のもの）を分ける。
4. **不確実性**: ASR の誤認識、カット検出の取りこぼし、画像から読めなかったものを明記。

デザインと言葉に厳しいユーザーなので、AI っぽい常套句（「〜と言えるでしょう」「効果的に活用」「視聴者を惹きつける」）は使わない。数字と時刻で語る。

## 5. 制約・既知の限界

- 外部サイトのメタデータ（再生数・チャンネル）は取れない。ユーザーに聞く。
- ASR は SenseVoice（中/英/日/韓/粤）。BGM が大きいと拾えない。`tags` に `BGM` `Laughter` などが出る。
- カット検出は輝度・色ヒストグラム差分。ディゾルブや緩いパンは1ショットに見える。
- テロップの文字認識は OCR ではなく、Claude がコンタクトシートを読む。細かい文字は keyframe で確認する。
